
# (1) Create RoS Base Template

ansible-playbook playbooks/create_ros_base_template.yml -i inventory/hosts -u root -e "target=node-1"

# (2) Add all hostnames and IPs to /etc/host on all VMs

ansible-playbook playbooks/local_DNS_hostfile.yml -u root -i inventory/hosts

# (3) Create Influxdb Server

ansible-playbook playbooks/setup_influxdb.yml -i hosts -e "target=influx"

# (4) Create PostgreSQL Master

ansible-playbook  playbooks/postgres_primary.yml -i inventory/hosts -e "pg_ver=12" -e "target=node-1"

# (5) Create Barman Server

# (6) Create Grafana Server


